  if peripheral.find("monitor") then shell.run("monitor right /.mon/startup")
   shell.run("monitor left .mon/startup")
    shell.run("monitor top .mon/startup")
     shell.run("monitor bottom .mon/startup")
      shell.run("monitor front .mon/startup")
       shell.run("monitor back .mon/startup")
          elseif not term.isColor then
      term.setBackgroundColour(colours.white)
      term.setTextColour(colours.black)
      term.clear()
   if fs.exists("APIs/graphix") then os.loadAPI("APIs/graphix")
      graphix.centerSlowPrint(8,"Whoops!")
       graphix.centerSlowPrint(9,"Please use GameStation 2 on a colour computer!")
        else term.setCursorPos(1,8)
        textutils.slowPrint("                        Whoops!")
        textutils.slowPrint(" Please use GameStation 2 on an advanced computer!")
         sleep(3)
          term.clear()
          end
      else os.loadAPI("APIs/graphix")
       term.setTextColor(colours.white)
       term.setBackgroundColor(colours.red)
       term.clear()
        graphix.centerSlowPrint(6,"Game Fusion Presents...")
         sleep(2)
          term.clear()
           term.setCursorPos(1,8)
            graphix.centerSlowPrint(6,"GameStation 2.")
            graphix.centerSlowPrint(7,"The Ultimate in CC Consoles.")
           sleep(3)
          shell.run("browser")
         end