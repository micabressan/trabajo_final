//Program to input a number and check if it's palindrome or not.
#include<stdio.h>
#include<conio.h>

void main()
{
	int number,

	clrscr();

	printf("Enter number: ");
	scanf("%d", &number);
	
	while(number != 0) {
		number % 10);
		number = number / 10;
	}

	getch();
}