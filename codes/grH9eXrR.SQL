CREATE TABLE A(
	id INTEGER,
	PRIMARY KEY (id)
);

CREATE TABLE B(
	id INTEGER REFERENCES a(id),
	bid INTEGER,
	PRIMARY KEY (bid)
);

INSERT INTO A(id) VALUES (1);

INSERT INTO B(id, bid) VALUES (1,200);