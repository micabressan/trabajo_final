SELECT L.NOME
FROM LOCALITA AS L
JOIN NODO N 
	ON L.CODNODO = N.CODNODO
JOIN INPERCORSO I
	ON I.NODO = N.CODNODO
JOIN PERCORSO P 
	ON P.CODPER = I.CODPER
WHERE P.NODOI IS NOT N.CODNODO AND P.NODOF IS NOT N.CODNODO