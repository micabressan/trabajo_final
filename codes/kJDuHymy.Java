if(spielfeldAlt[0][0] == '.' && (for(int i = 0; i < spielfeldAlt.length ; i++) {
                    for(int b = 0; b < spielfeldAlt[i].length ; b++) {
                        spielfeldAlt[i][b] == 'A';
                    }) || (for(int i = 0; i < spielfeldAlt.length ; i++) {
                    for(int b = 0; b < spielfeldAlt[i].length ; b++) {
                        spielfeldAlt[i][b] == 'B';
                    })
                }) {
                    break;
            }