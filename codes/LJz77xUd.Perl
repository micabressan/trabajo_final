#!/usr/bin/perl
foreach($ia=0;$ia++;$ia<=@rlyeh)
{$cthulhu = sleeping($rlyeh[$ai]) or dead($cthulhu) unless !right{stars}}
for($ia==0;$ia++;$ia!=1000)
{
   $shub'niggurath->{stargoat}->{child}->[$ia] = ++$ia;
  $ai = $ia - 1;
}

$side = 0;
$square[$side++]++ until $side>4;

# Eyes! And Teeth! In the darkness! IA!