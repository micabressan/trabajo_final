square = (x) -> x*x

l = [1,2,3,4]

printL = (curList) ->
    res = ""
    for x in curList
        res = res + " " +x
    res


myFunc = (curList) ->
    for x in curList
        do (x) ->
            square (x) ->
                x+3

alert (printL (myFunc l))